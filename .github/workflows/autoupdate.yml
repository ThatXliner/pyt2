name: Autoupdate
on:
  push:
  workflow_dispatch:
  schedule:
    - cron: "0 0 1 * *"  # Run every month
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: Set up Python 3.9
        uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: Install dependencies
        run: python3 -m pip install --upgrade pip cookiecutter poetry poetry-plugin-up
      - name: Update template
        run: make all
      - name: Check if we should commit
        id: commit_check
        run: |
          if git diff --quiet
          then
            echo "should_commit==false" >> $GITHUB_OUTPUT
          else
            echo "should_commit==true" >> $GITHUB_OUTPUT
          fi
      - name: Commit updated lockfiles
        uses: EndBug/add-and-commit@v7
        if: ${{ contains(steps.commit_check.outputs.should_commit, 'true') }}
        with:
          default_author: github_actions
          message: ":robot: :arrow_up: Updated deps and lockfile\n\nThis is an automated action"
